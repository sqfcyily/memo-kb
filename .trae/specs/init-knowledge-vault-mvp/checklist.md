- [x] Project initialized with Next.js App Router and dependencies installed.
- [x] Git username configured as `xiaomajiang` and commit messages set to English.
- [x] Directory structure (`inbox/`, `library/`, `.kv-meta/`) created successfully.
- [x] Capture API saves raw files with timestamp and UUID in `inbox/`.
- [x] LLM Service generates valid JSON based on the specified schema.
- [x] Auto-archive successfully creates new library markdown files without user confirmation.
- [x] Merge confirmation provides diff and patches target files accurately upon approval.
- [x] SQLite FTS5 indexes files and supports fast searching.
- [x] UI provides quick capture and single-box search functionality.
- [x] UI displays Inbox and Library items properly, rendering Markdown.
- [x] CLI `kb` successfully queries the local index and returns text results.
